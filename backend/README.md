# backend

The backend contains 3 versions depending on the method of deployment

1. agentcore - to be deployed using the AgentCore CLI
2. cdk - to be deployed using AWS CDK in the /infrastructure folder
3. vps - to be deployed on a VPS running with docker-compose
